import { Text, View } from "react-native";

// 👇 Comment this out to get a working web bundle. Leave it imported and the web bundle will break.

function Index() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Base</Text>
    </View>
  );
}

export default Index;
